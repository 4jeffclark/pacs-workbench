# TradingCoach Python library

Shared stdlib-only helpers for `layer1-skills/*/scripts/run.py`.

Skill scripts resolve this path:

```text
../../assets/tc-lib
```

(relative to `trading-coach.app/` instance root)
