# compose-greeting scripts

Bundled executables for the `compose-greeting` skill. Run from the skill directory per agentskills.io conventions.

| Script | Purpose |
| --- | --- |
| `run.py` | Compose greeting artifacts in the agent workspace |
