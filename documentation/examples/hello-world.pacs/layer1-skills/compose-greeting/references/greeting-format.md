# Greeting format

Core greeting is a single sentence:

```text
Hello, {recipient}!
```

Use the resolved playbook input `recipient`. Do not add punctuation variants unless the user explicitly requests a different tone.
